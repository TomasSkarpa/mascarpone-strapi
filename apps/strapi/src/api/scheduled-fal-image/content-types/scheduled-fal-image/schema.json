{
  "kind": "singleType",
  "collectionName": "scheduled_fal_images",
  "info": {
    "singularName": "scheduled-fal-image",
    "pluralName": "scheduled-fal-images",
    "displayName": "FAL AI",
    "description": "Server-side fal generation on an interval; use the FAL AI output page section on pages to show the latest image."
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {},
  "attributes": {
    "enabled": {
      "type": "boolean",
      "default": false
    },
    "falModelId": {
      "type": "string",
      "required": false
    },
    "prompt": {
      "type": "text",
      "required": false
    },
    "sourceImage": {
      "type": "media",
      "multiple": false,
      "required": false,
      "allowedTypes": ["images"]
    },
    "outputImage": {
      "type": "media",
      "multiple": false,
      "required": false,
      "allowedTypes": ["images"]
    },
    "mediaUploadFolderName": {
      "type": "string",
      "default": "fal-history",
      "required": false,
      "description": "Existing Media Library folder name (create it under Media Library first)."
    },
    "minIntervalHours": {
      "type": "integer",
      "default": 48,
      "min": 1
    },
    "lastGeneratedAt": {
      "type": "datetime"
    },
    "lastJobStartedAt": {
      "type": "datetime"
    },
    "lastJobStatus": {
      "type": "enumeration",
      "enum": ["idle", "running", "success", "error"],
      "default": "idle"
    },
    "lastError": {
      "type": "text"
    }
  }
}
